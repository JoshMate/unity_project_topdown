using UnityEngine;

public class F_Logic_ProfileManager : MonoBehaviour
{

    [Header("Object Refs")]
    public F_Logic_Profile_Inventory profileInventoryObject;
    public F_Logic_Profile_Stats profileStatsObject;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
